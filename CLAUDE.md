## Agent skills

### Issue tracker

Issues tracked as GitHub Issues (repo: mfmanca2000/JiraChromeExtension), via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (root CONTEXT.md + docs/adr/, created lazily). See `docs/agents/domain.md`.
